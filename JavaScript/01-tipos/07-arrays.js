// Array literal es usando []
let animales = ['gato', 'cisne'];
console.log(animales);

// Para mostrar un solo elemento
console.log(animales[0]);

// Agregar elementos
animale[2] = 'Golondrina';

// Tener cuidado con añadir elementos mediante el indice (Los numeros asignados a los valores), por ejemplo si solo tiene 3 no pongas 7, ve en orden (EL INDICE EMPIEZA EN 0)